services:
  - type: web
    name: ray-serve-app
    env: python
    buildCommand: pip install -r requirements.txt && python src/train_ray.py
    startCommand: python src/serve_ray.py
    # PORT is automatically set by Render, no need to specify
    healthCheckPath: /predict

